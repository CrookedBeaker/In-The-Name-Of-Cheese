/// @description Gone.

if (other.object_index != obj_floorobstacle) {
	instance_destroy();
	
	if (!global.muteSound && sprite_index = spr_bullet && !audio_is_playing(snd_gun_wall)) {
		audio_play_sound(snd_gun_wall,10,false);
	}
}